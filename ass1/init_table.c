#include <stdio.h>
#include <stdlib.h>

#include "process.h"

// initiate the process table using singl linked list and return the head's address

process * init_table(){

  process * head = NULL; // caution: possible memory leak
  return head;
}
